// import * as core from '../../src/index';

/*describe('Model Util Test Suite', () => {
  const createModelDTO = {
    resource_id: '1',
    resource_type: 'type1',
    description: 'desc1',
  };

  const updateModelDTO = {
    resource_id: '1',
    resource_type: 'type2',
    description: 'desc2',
  };

  const props = {
    resourceId: {
      column: 'resource_id',
    },
    resourceType: {
      column: 'resource_type',
    },
    description: {
      column: 'description',
    },
  };

  const instance: { [index: string]: any } = {};

  it('create model', () => {
    core.createModelByDTO(instance, props, [createModelDTO]);
    expect(instance.resourceId).toBe('1');
    expect(instance.resourceType).toBe('type1');
    expect(instance.description).toBe('desc1');
  });

  it('update model', () => {
    core.updateModelByDTO(instance, props, [updateModelDTO]);
    expect(instance.resourceId).toBe('1');
    expect(instance.resourceType).toBe('type2');
    expect(instance.description).toBe('desc2');
  });
});*/
